services:
  - type: web
    name: frueher-oder-spaeter-backend
    env: node
    plan: free
    region: frankfurt
    rootDir: server
    buildCommand: "npm install"
    startCommand: "npm run start"
    envVars:
      - key: NODE_ENV
        value: production
